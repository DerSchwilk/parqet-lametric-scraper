Simple python web-server querying parqet API and transforming the JSON to LaMetric My DATA DIY readable.

Build docker image with ```docker build . -t "parqt-lametric" --build-arg PORTFOLIO_ID=${PORTFOLIO_ID}```
